# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.claude/settings.local.json

# Volcados/backups de la base de datos: contienen datos sensibles (todas las
# finanzas de los usuarios). NUNCA subir al repo. Los .sql de esquema SÍ se
# versionan (no coinciden con estos patrones).
backup_*.sql
*.dump
/backups/
# Credenciales de conexión que puedas dejar en local para el backup
.pg-credenciales
.env.backup

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.Rhistory
